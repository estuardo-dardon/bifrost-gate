global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: 'bifrost-gate'
    basic_auth:
      username: 'prometheus'
      password: 'SecretPassword123'
    static_configs:
      - targets: ['bifrost-gate:2704']
